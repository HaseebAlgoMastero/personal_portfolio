[build]
  command = "npm run build"
  publish = "build/client"

[build.environment]
  NODE_VERSION = "20.12.2"

# SPA-style fallback so deep links work on static hosting
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
